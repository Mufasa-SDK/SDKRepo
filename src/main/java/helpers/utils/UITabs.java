package helpers.utils;

/**
 * Defines the type of options available when querying for a tab of the game UI.
 */
public enum UITabs {
    ACCOUNT,
    CLAN,
    COMBAT,
    EMOTES,
    EQUIP,
    FRIENDS,
    INVENTORY,
    LOGOUT,
    MAGIC,
    MUSIC,
    PRAYER,
    QUESTS,
    SETTINGS,
    STATS
}
